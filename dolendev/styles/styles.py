import reflex as rx
from enum import Enum
from .colors import Color
from .colors import TextColor

# Constants
MAX_WIDTH = "600px"

# Sizes

class Size(Enum):
    SMALL="0.5em"
    MEDIUM="0.8em"
    DEFAULT="1em"
    MBIG="1.5em"
    BIG="2em"
